# CLAUDE.md — 커뮤니티 숏츠 생성기

이 프로젝트는 유튜브 쇼츠(9:16, 1080x1920)용 "익명 커뮤니티 게시글" 형식 영상을
**대량으로, 반복해서** 만들기 위한 재사용 가능한 생성기다. 영상 한 편을 만드는
프로젝트가 아니라, `content.json`만 바꿔서 계속 새 영상을 뽑아내는 **템플릿**이다.

앞으로 이 저장소에서 새 영상을 만들거나 기능을 수정할 때는 아래 원칙을 반드시
지킨다.

## 제작 원칙 (반드시 유지)

1. **실제 커뮤니티 서비스를 그대로 복제하지 않는다.**
   특정 서비스의 로고, 색상 조합, 폰트, 아이콘을 그대로 베끼지 않는다. "익숙한
   한국 인터넷 커뮤니티 느낌"을 주는 가상의 UI를 유지한다.
2. **영상마다 디자인을 새로 만들지 않는다.**
   `index.html` / `styles.css` / `script.js`는 모든 영상이 공유하는 엔진이다.
   특정 대본에만 맞춘 1회성 스타일 수정을 넣지 않는다.
3. **기본 템플릿을 유지한다.**
   헤더 → 제목 → 본문(문단 단위 등장) → (선택) 이미지 → 강조 장면 → 결론 →
   마지막 댓글, 이 흐름 자체를 바꾸지 않는다.
4. **대본과 이미지와 타이밍 데이터만 교체한다.**
   새 영상은 오직 `content.json` (그리고 필요하면 `assets/` 파일)만 수정해서
   만든다. HTML/CSS/JS에 특정 영상의 문구를 하드코딩하지 않는다.
5. **한 장면에 너무 많은 문장을 넣지 않는다.**
   본문 한 그룹(`bodySections[i]`)은 최대 3~4줄을 넘기지 않는다.
6. **모바일 가독성을 최우선으로 한다.**
   본문 폰트는 최소 48px 이상, 제목은 굵고 크게, 줄 수는 짧게. 과도한 장식·
   그림자·그라디언트로 가독성을 해치지 않는다.
7. **AI 이미지 남발을 피한다.**
   이미지가 없어도 레이아웃이 깨지지 않아야 하고, 있어도 최소한으로만 쓴다.
8. **한 영상에 상황 이미지 0~2장이 기본이다.**
   `content.json`의 `image.src`는 기본값이 `null`이며, 꼭 필요할 때만 채운다.

## 애니메이션 원칙

- 사용하는 모션은 fade, slide-up, 아주 약한 zoom(scale), 핵심 단어 scale,
  댓글 slide-up 정도로 제한한다. 화려한 모션(회전, 바운스, 파티클 등)을
  추가하지 않는다.
- 등장/퇴장 트랜지션 길이는 150~300ms 사이를 유지한다 (`script.js`의
  `ENTER_MS`, `EXIT_MS`).
- `script.js`의 `renderAtTime(t)`는 **임의의 시각 t로 즉시 이동해서 그리는
  순수 함수**여야 한다. 실시간 애니메이션 루프와 프레임 캡처(Playwright)가
  이 함수 하나를 공유하기 때문에, 여기에 실시간 타이머나 브라우저 전용 API에
  의존하는 로직을 넣지 않는다.

## 데이터 구조 (content.json)

모든 영상 콘텐츠는 `content.json` 하나에만 존재한다. 필드를 임의로 이름을
바꾸거나 구조를 흔들지 않는다 (기존 영상들의 재현성이 깨진다).

- `meta`: 커뮤니티 이름/게시 시간/영상 길이(`durationMs`)/프레임레이트(`fps`)
- `title`: 게시글 제목 + 노출 구간
- `bodySections[]`: 문단 단위 블록. 각 블록은 `lines[]`를 가지며, 각 줄은
  **자기 자신의 시작/종료 시각**을 가진다 (현재 읽히는 문장 강조에 사용).
- `emphasis`: 레이아웃을 깨고 화면 중앙에 크게 보여줄 한 문장
- `conclusion`: 마무리 문장들 + 가장 강하게 강조할 단어(`strongText`)
- `comment`: 마지막에 아래에서 올라오는 댓글 (TTS로 읽지 않는다)
- `image`: 첨부 이미지 (없으면 `src: null`, 레이아웃 유지됨)
- `audio.narrationSrc`: 내레이션 오디오 파일 경로 (선택)

새 영상을 만들 때는 이 스키마를 그대로 두고 **값만** 교체한다.

## 새 영상 만드는 절차

1. `content.json`을 새 대본에 맞게 수정한다 (본문은 3~4줄 단위로 쪼개고,
   각 줄/구간의 `startTime`/`endTime`을 순서대로 채운다).
2. 필요하면 `assets/images/`, `assets/audio/`에 이미지·오디오 파일을 추가하고
   `content.json`에서 경로를 참조한다.
3. `npm run dev`로 브라우저에서 실시간 재생을 확인한다.
4. `npm run render` (또는 `npm run video`)로 프레임을 캡처하고, ffmpeg가
   설치되어 있으면 `output/video.mp4`까지 자동 생성한다.

## 하지 말아야 할 것

- `index.html`/`styles.css`/`script.js`에 특정 영상의 텍스트를 직접 넣는 것
- 특정 실존 커뮤니티 서비스의 로고/브랜드를 사용하는 것
- 한 화면에 5줄 이상의 본문을 한 번에 노출하는 것
- 150~300ms 범위를 벗어나는 과한 트랜지션이나 화려한 모션 추가
- `renderAtTime(t)`가 실시간 상태(예: 이전 프레임과의 차이)에 의존하게 만드는 것
  (항상 절대 시각 t만으로 결과가 결정되어야 캡처가 정확해진다)
